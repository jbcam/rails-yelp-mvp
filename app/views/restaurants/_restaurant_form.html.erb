<h2>Add Restaurant</h2>
<%= simple_form_for(@restaurant, html: { class: 'form' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category %>
  <%= f.button :submit %>
<% end %>

